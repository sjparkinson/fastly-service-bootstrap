{
  "name": "fastly-service-bootstrap",
  "version": "1.0.0",
  "description": "Project framework for a Fastly service configured with Terraform.",
  "author": "Samuel Parkinson <samuel.parkinson@ft.com>",
  "private": true,
  "scripts": {
    "fmt": "terraform fmt && prettier --write 'test/**/*.js'",
    "test": "mocha --timeout 60000"
  },
  "license": "MIT",
  "dependencies": {
    "chai": "^4.1.1",
    "chai-http": "^3.0.0",
    "mocha": "^3.5.0",
    "prettier": "^1.5.3"
  }
}
